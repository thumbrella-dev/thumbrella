//! Pre-flight build checks for Thumbrella.
//! This crate has zero runtime code — its build.rs runs first to verify
//! the build environment before any dependencies are compiled.

// Cargo requires at least one source file.  All logic is in build.rs.
